<main class="container">
  <header>
    <h1>記事を投稿する</h1>
  </header>
  <%= form_for @article do |f| %>
    <% if @article.errors.any? %>
      <div class="bg-danger">
        <p><%= @article.errors.count %>件のエラーのため保存できませんでした</p>
        <ul>
          <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <% if @categories.exists? %>
    <div class="form-group">
      <%= f.label :category_id, "記事カテゴリ" %>
      <%= f.select :category_id, Category.all.map{ |c| [c.name, c.id]} %>
    </div>

    <div class="form-group">
      <%= f.label :title, "記事タイトル" %>
      <%= f.text_field :title, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :content, "ブログ本文" %>
      <%= f.text_area :content, class: "form-control" %>
    </div>

    <%= f.submit "投稿する", class: "btn btn-primary" %>
    <%= link_to "戻る", articles_path, class: "btn btn-default" %>
    <% else %>
    <p>このブログにはまだカテゴリがありません。まずはカテゴリを作ってください！</p>
    <%= link_to "カテゴリを作る", new_category_path, class: "btn btn-default" %>
    <% end %>
  <% end %> 
</main>